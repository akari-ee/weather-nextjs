// import { NextResponse } from 'next/server';

// export async function middleware(req: any) {
//   const { nextUrl: url, geo } = req;
//   url.searchParams.set('geo', geo.country);
//   console.log(url);
//   return NextResponse.rewrite(url);
// }
